'use strict';
let hasDriverslicense = false;
const passTest = true;

if (passTest) hasDriverLicense = true; //This is an error an use strict beacuse, we have to declare the variable with const, let or var
if (hasDriversLicense) console.log('I can Drive :D');

const interface = 'Audio';
